import React from 'react'
import { Grid, Image } from 'semantic-ui-react'

const WorkExp = (props) => (
    <Grid>
        <Grid.Column width={4}>
            <Image src={props.work.image} />
        </Grid.Column>
        <Grid.Column width={9}>
            Picture removal detract earnest is by. Esteems met joy attempt way clothes yet demesne tedious.
            Replying an marianne do it an entrance advanced. Two dare say play when hold.
            Required bringing me material stanhill jointure is as he. Mutual indeed yet her living result matter him bed whence.
    </Grid.Column>
    </Grid>
)

export default WorkExp